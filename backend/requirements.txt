# FastAPI backend
fastapi
uvicorn[standard]

# Database (SQLAlchemy ORM)
sqlalchemy
psycopg2-binary

# Environment variables
python-dotenv

# Web scraping (Playwright)
playwright

# HTTP requests (fallback scraping / APIs)
requests
httpx

# HTML parsing (backup for scraping)
beautifulsoup4
lxml

# Async support (important for FastAPI + scraping)
aiohttp

# Data handling
pandas
numpy

# Optional but useful for scheduling / utilities
schedule